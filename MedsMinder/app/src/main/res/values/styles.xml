<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="InfoDialogSessionTitle" parent="@style/TextAppearance.AppCompat.Notification.Title">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:background">@color/colorTextGray</item>
        <item name="android:padding">@dimen/view_margin_general</item>
    </style>

    <style name="WidgetTitle" parent="@style/TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/colorWhite</item>
    </style>


</resources>
